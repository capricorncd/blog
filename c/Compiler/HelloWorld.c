#include <stdio.h>

int main()
{
    printf("更快、更高、更强、更团结(Faster, Higher, Stronger, Together)!\n");
    return 0;
}

// 编译成可执行文件
// gcc HelloWorld.c -o HelloWorld
// 执行编译后的文件
// ./HelloWorld
// 输出结果：更快、更高、更强、更团结(Faster, Higher, Stronger, Together)!